# SQLite version 3.x
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
development:
  adapter: mysql
  database: blog_aggregator_development
  username: root
  password: password
  host: localhost
  pool: 5
  timeout: 5000

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: mysql
  database: blog_aggregator_test
  username: root
  password: password
  host: localhost
  pool: 5
  timeout: 5000

production:
  adapter: pg
  database: blog_aggregator
  # username: root
  # password: password
  # host: localhost
  pool: 5
  timeout: 5000
