<%= form_for(@blog) do |f| %>
  <% if @blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% @blog.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :blogger_name %><br />
    <%= f.text_field :blogger_name %>
  </div>
  <div class="field">
    <%= f.label :blog_url %>(Please Mention  http:// or https:// )<br />
    <%= f.text_field :blog_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
